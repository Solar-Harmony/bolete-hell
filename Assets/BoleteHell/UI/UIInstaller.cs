using Zenject;

namespace BoleteHell.Code.UI
{
    public class UIInstaller : Installer
    {
        public override void InstallBindings()
        {
            // Add UI-specific bindings here when needed
            // Currently no UI services are being bound in the original installer
        }
    }
}
